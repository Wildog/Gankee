language: objective-c
osx_image: xcode8.2
branches:
  only: 
    - master
script:
    - xcodebuild clean build -sdk iphonesimulator -workspace Gankee.xcworkspace -scheme Gankee CODE_SIGNING_REQUIRED=NO
